{
  "name": "livekit-token-generator",
  "version": "1.0.0",
  "description": "Generate LiveKit tokens for development",
  "private": true,
  "scripts": {
    "generate": "node generate_token.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "livekit-server-sdk": "^2.7.2"
  }
}

