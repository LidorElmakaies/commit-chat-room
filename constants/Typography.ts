import { TextStyle } from "react-native";

export const Typography = {
  title: {
    fontSize: 28,
    fontWeight: "bold",
    marginBottom: 8,
    textAlign: "center",
  } as TextStyle,
  subtitle: {
    fontSize: 16,
    marginBottom: 32,
    textAlign: "center",
  } as TextStyle,
};

export default Typography;

